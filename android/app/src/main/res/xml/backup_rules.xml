<?xml version="1.0" encoding="utf-8"?>
<!-- Shared preferences hold the user session and profile data; they are
     device-local and must never end up in a cloud backup or device transfer. -->
<full-backup-content>
    <exclude domain="sharedpref" path="." />
</full-backup-content>
